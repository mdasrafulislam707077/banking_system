import { ActiveMobileNavDirective } from './active-mobile-nav.directive';

describe('ActiveMobileNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveMobileNavDirective();
    expect(directive).toBeTruthy();
  });
});
